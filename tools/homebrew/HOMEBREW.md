# Homebrew

## Generate Homebrew Formula

To test the script, run it here:

```
python generate_homebrew_formula.py --package easyborg --version 0.15.1 --python-version 3.14 --template easyborg.rb.template --output easyborg.rb
```

The script doesn't install anything so it can run in the normal venv without problems.